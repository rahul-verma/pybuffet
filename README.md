### PyBuffet

A collection of context-free Python utilities for simple tasks.

The repository is meant to be used during workshop discussions on higher level concepts. Hence, the focus on design itself is minimal to keep the things simple.
